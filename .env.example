# DEBUG=true
# CORS_ORIGINS=http://localhost:3000,https://your-domain.example
# DATABASE_URL=sqlite:////absolute/path/to/jaegun.db
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/jaegun

# 관리자(공식 공지·일정·계획 수정). 반드시 강한 값으로 변경
ADMIN_TOKEN=busan_youth_jaegun

# 배포·Docker: static/, data/ 가 있는 디렉터리(절대 경로). 예: /app
# JAEGUN_PROJECT_ROOT=/absolute/path/to/jaegun
# 구 호환(프로젝트 루트만 지정할 때)
# JAEGUN_STATIC_ROOT=/absolute/path/to/jaegun
